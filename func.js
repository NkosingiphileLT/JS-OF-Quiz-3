//Create a register function:
//Create a user object 
// Push this user object onto an array

function register (user){
   user.push(user);
    }

    register({
        uname : 'Lutho',
        email : 'lutho2gmaiil.conm',
        password: 'VNL'
    })

    console.log(user)

